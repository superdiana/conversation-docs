{
  "name": "conversation-docs",
  "version": "1.0.0",
  "devDependencies": {
    "git-scripts": "^0.2.1",
    "surge": "^0.19.0"
  },
  "git": {
    "scripts": {
      "pre-push": "surge --project ./ --domain conversation-docs.surge.sh"
    }
  }
}
